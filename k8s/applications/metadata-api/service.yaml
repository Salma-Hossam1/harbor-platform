apiVersion: v1
kind: Service
metadata:
  name: metadata-api
  namespace: artifact-platform
spec:
  selector:
    app: metadata-api

  ports:
    - name: http
      port: 8081
      targetPort: http

  type: ClusterIP