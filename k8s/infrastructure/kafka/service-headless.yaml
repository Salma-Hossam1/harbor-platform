apiVersion: v1
kind: Service

metadata:
  name: kafka-headless
  namespace: artifact-platform

spec:
  clusterIP: None

  selector:
    app: kafka

  ports:
    - name: kafka
      port: 9092
      targetPort: kafka