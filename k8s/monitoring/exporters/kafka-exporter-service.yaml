apiVersion: v1
kind: Service
metadata:
  name: kafka-exporter
  namespace: artifact-platform
spec:
  type: ClusterIP
  selector:
    app: kafka-exporter
  ports:
    - name: metrics
      port: 9308
      targetPort: metrics