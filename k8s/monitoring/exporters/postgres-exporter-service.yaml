apiVersion: v1
kind: Service
metadata:
  name: postgres-exporter
  namespace: artifact-platform

spec:
  type: ClusterIP

  selector:
    app: postgres-exporter

  ports:
    - name: metrics
      port: 9187
      targetPort: metrics