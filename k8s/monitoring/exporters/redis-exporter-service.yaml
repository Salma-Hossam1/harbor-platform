apiVersion: v1
kind: Service
metadata:
  name: redis-exporter
  namespace: artifact-platform

spec:
  type: ClusterIP

  selector:
    app: redis-exporter

  ports:
    - name: metrics
      port: 9121
      targetPort: metrics