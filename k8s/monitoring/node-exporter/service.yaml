apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: artifact-platform

spec:
  type: ClusterIP
  clusterIP: None

  selector:
    app: node-exporter

  ports:
    - name: metrics
      port: 9100
      targetPort: metrics